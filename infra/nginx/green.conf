# ENH-INFRA-006 — Green slot upstream definition
# Referenced by router.conf when ACTIVE_SLOT=green

upstream green_gateway {
    server gateway-api-green:80 max_fails=3 fail_timeout=10s;
    keepalive 32;
}
