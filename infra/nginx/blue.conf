# ENH-INFRA-006 — Blue slot upstream definition
# Referenced by router.conf when ACTIVE_SLOT=blue

upstream blue_gateway {
    server gateway-api-blue:80 max_fails=3 fail_timeout=10s;
    keepalive 32;
}
